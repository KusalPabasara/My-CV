name: Compile LaTeX CV

on:
  push:
    paths:
      - '**.tex'
      - '.github/workflows/compile-latex.yml'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Setup LaTeX and Compile
      uses: xu-cheng/latex-action@v2
      continue-on-error: true
      with:
        root_file: cv.tex
        latexmk_use_xelatex: true
        pre_compile: |
          tlmgr update --self
          tlmgr install \
            xcolor geometry \
            babel latexmk etoolbox \
            tikz-cd pgf enumitem \
            fontawesome5 moderncv fontspec

    - name: Commit compiled PDF
      run: |
        git config --global user.name "github-actions"
        git config --global user.email "github-actions@github.com"

        if [ -f cv.pdf ]; then
          git add cv.pdf
          git commit -m "Auto-compile cv.pdf from LaTeX" || echo "Nothing to commit"
          git push || echo "Nothing to push"
        else
          echo "cv.pdf not found, skipping commit."
        fi
